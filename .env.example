# Hoxtup — Shared Environment Variables
# Copy to .env and adjust values as needed

# PostgreSQL (used by docker-compose and Hoxtup-api)
POSTGRES_USER=hoxtup
POSTGRES_PASSWORD=hoxtup_dev
POSTGRES_DB=hoxtup_dev

# See Hoxtup-api/.env.example and Hoxtup-app/.env.example for project-specific variables
